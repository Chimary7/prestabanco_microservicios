eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-service-service:8761/eureka/
spring:
  application:
    name: userservice
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://userservice-db-service:5432/microprestabancouser
    username: postgres
    password: Vd8901
  jpa:
    hibernate:
      ddl-auto: update