spring:
  application:
    name: EvaluationService
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/microprestabancoevaluation
    username: postgres
    password: Vd8901
  jpa:
    hibernate:
      ddl-auto: update