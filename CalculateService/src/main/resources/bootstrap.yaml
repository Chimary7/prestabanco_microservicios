spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: CalculateService
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/microprestabancocalculate
    username: postgres
    password: Vd8901
  jpa:
    hibernate:
      ddl-auto: update