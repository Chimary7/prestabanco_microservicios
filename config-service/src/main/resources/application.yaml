server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Chimary7/prestabanco_microservicios.git
          search-paths: config-data
  application:
    name: config-service
